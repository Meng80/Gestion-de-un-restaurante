<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springboot.mapper.DishCategoryMapper">
    <insert id="insert">
        insert into dish_category(name, sort, status, create_time, update_time, create_user, update_user)
        VALUES (#{name}, #{sort}, #{status}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <delete id="deleteById">
        delete from dish_category where id = #{id}
    </delete>

    <update id="update" parameterType="com.example.springboot.entity.DishCategory">
        update dish_category
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="list" resultType="com.example.springboot.entity.DishCategory">
        select * from dish_category
        where status = 1
        order by sort asc,create_time desc
    </select>
</mapper>
