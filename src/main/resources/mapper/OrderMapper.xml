<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.springboot.mapper.OrderMapper">

    <insert id="insert" parameterType="com.example.springboot.entity.Orders" useGeneratedKeys="true" keyProperty="id">
        insert into orders
        (status, number_order,number_mesa,order_time, checkout_time, pay_method, pay_status, amount, remark,
         delivery_time)
        values (#{status}, #{numberOrder}, #{numberMesa}, #{orderTime}, #{checkoutTime}, #{payMethod},
                #{payStatus}, #{amount}, #{remark}, #{deliveryTime})
    </insert>

    <update id="update">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' ">
                cancel_reason=#{cancelReason},
            </if>
            <if test="cancelTime != null">
                cancel_time=#{cancelTime},
            </if>
            <if test="payStatus != null">
                pay_status=#{payStatus},
            </if>
            <if test="payMethod != null">
                pay_method=#{payMethod},
            </if>
            <if test="checkoutTime != null">
                checkout_time=#{checkoutTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deliveryTime != null">
                delivery_time = #{deliveryTime}
            </if>
        </set>
        where id=#{id};
    </update>

    <select id="getByNumberAndNumberMesa" resultType="com.example.springboot.entity.Orders">
        select *
        from orders
        where number = #{numberOrder}
          and number_mesa = #{numberMesa};
    </select>


    <select id="getById" resultType="com.example.springboot.entity.Orders">
        select *
        from orders
        where id = #{id};
    </select>

    <select id="countStatus" resultType="java.lang.Integer">
        select count(*)
        from orders where status=#{status};
    </select>

    <select id="getByStatusAndOrderTime" resultType="com.example.springboot.entity.Orders">
        select *
        from orders where status=#{status} and order_time&lt;#{orderTime};
    </select>

    <select id="sumByMap" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
    </select>


    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
    </select>


    <select id="getSalesTop10" resultType="com.example.springboot.controller.dto.GoodsSalesDTO">
        select od.name name,sum(od.number) number from order_detail od ,orders o
        where od.order_id = o.id
        and o.status = 5
        <if test="begin != null">
            and order_time &gt;= #{begin}
        </if>
        <if test="end != null">
            and order_time &lt;= #{end}
        </if>
        group by name
        order by number desc
        limit 0, 10
    </select>

    <select id="getByNumberOrder" resultType="com.example.springboot.entity.Orders">
        select *
        from orders
        where number_order = #{numberOrder}
    </select>
</mapper>